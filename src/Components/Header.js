import React from 'react';

const Header = () => {
  return (
    <header>
      <h1>SUPER MEMORY</h1>
      <p>
        Click on cards to gain points. Be sure not to click on a card more than
        once.
      </p>
    </header>
  );
};

export default Header;
